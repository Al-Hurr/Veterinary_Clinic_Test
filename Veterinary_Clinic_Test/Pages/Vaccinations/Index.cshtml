@page
@model Veterinary_Clinic_Test.Pages.Vaccinations.IndexModel
@{
}

<br />
<div class="container row p-0 m-0">
    <div class="col-9">
        <h2 class="text-info text-black-50">Список прививок</h2>
    </div>
    <div class="col-3">
        <a class="btn btn-info form-control text-white" asp-page="/Vaccinations/Create">Добавить прививку</a>
    </div>
    <div class="col-12 border mt-3 p-3">
        <form method="post">
            @if (Model.Vaccinations.Any())
            {
                <table class="table table-striped table-bordered border">
                    <tr class="table-secondary thead-dark">
                        <th>
                            <label asp-for="@Model.Vaccinations.FirstOrDefault().Name"></label>
                        </th>
                        <th>
                            <label asp-for="@Model.Vaccinations.FirstOrDefault().Duration"></label>
                        </th>
                        <th>
                            <label asp-for="@Model.Vaccinations.FirstOrDefault().Cost"></label>
                        </th>
                        <th>
                            <label asp-for="@Model.Vaccinations.FirstOrDefault().Doctor"></label>
                        </th>
                        <th>
                            <label asp-for="@Model.Vaccinations.FirstOrDefault().Animal"></label>
                        </th>
                        <th>
                            <label asp-for="@Model.Vaccinations.FirstOrDefault().Diagnosis"></label>
                        </th>
                        <th class="w-25"></th>
                    </tr>
                    @foreach (var item in Model.Vaccinations)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(m => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.Duration)
                            </td>
                            <td>
                                @Html.DisplayFor(m => item.Cost)
                            </td>
                            <td>
                                @(item.Doctor == null ? "-пусто-" : Html.DisplayFor(m => item.Doctor.FullName)) 
                            </td>
                            <td>
                                @(item.Animal == null ? "-пусто-" : Html.DisplayFor(m => item.Animal.Name))
                            </td>
                            <td>
                                @(item.Diagnosis == null ? "-пусто-" : Html.DisplayFor(m => item.Diagnosis.Name))
                            </td>
                            <td style="text-align:center;">
                                <a asp-page="Edit" asp-route-id="@item.Id" class="btn btn-success btn-sm">Изменить</a>
                                <button onclick="return confirm('Вы хотите удалить строку?')" asp-route-id="@item.Id"
                                        asp-page-handler="Delete" class="btn btn-danger btn-sm">
                                    Удалить
                                </button>
                            </td>
                        </tr>
                    }
                </table>
            }
            else
            {
                <p>Список пуст</p>
            }
        </form>
    </div>
</div>
